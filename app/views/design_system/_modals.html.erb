<%#  Modals section of the living style guide - STUB. Wave 2 fills this with live
     specimens of the shared modal host, hold-to-confirm flows, and the reusable
     modal cards (studio-engine v0.4.5+). Left as a real partial so the section
     nav + anchor already work. %>
<section id="modals" class="space-y-5" style="scroll-margin-top: calc(var(--nav-h, 0px) + 5rem)">
  <div class="space-y-1">
    <h2 class="text-2xl font-bold text-heading">Modals</h2>
    <p class="text-muted text-sm">The shared modal host, hold-to-confirm flows, and the reusable modal cards.</p>
  </div>
  <div class="card p-8 text-center space-y-3" data-stub="true">
    <span class="badge" style="color: var(--color-cta); border-color: var(--color-cta)">Next slice</span>
    <p class="text-heading font-semibold">This section lands in the next build.</p>
    <p class="text-muted text-sm max-w-md mx-auto">
      Wave 2 fills it with live specimens of the modal host, hold modals, and the card blocks.
    </p>
  </div>
</section>
