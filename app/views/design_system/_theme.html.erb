<%#  Theme section of the living style guide - STUB. Wave 2 fills this with the
     7-role theme system, the theme editor, light/dark parity, and the stage-*
     badge palette. Left as a real partial so the section nav + anchor already
     work. %>
<section id="theme" class="space-y-5" style="scroll-margin-top: calc(var(--nav-h, 0px) + 5rem)">
  <div class="space-y-1">
    <h2 class="text-2xl font-bold text-heading">Theme</h2>
    <p class="text-muted text-sm">The 7-role theme system, light/dark parity, and the stage-* badge palette.</p>
  </div>
  <div class="card p-8 text-center space-y-3" data-stub="true">
    <span class="badge" style="color: var(--color-cta); border-color: var(--color-cta)">Next slice</span>
    <p class="text-heading font-semibold">This section lands in the next build.</p>
    <p class="text-muted text-sm max-w-md mx-auto">
      Wave 2 fills it with the role-token system, the theme editor, and the shared stage-badge palette.
    </p>
  </div>
</section>
