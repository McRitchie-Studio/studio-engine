<%#  Tasks section of the living style guide - STUB. Wave 2 fills this with the
     task cards, stage badges, and board status chips. Left as a real partial so
     the section nav + anchor already work. %>
<section id="tasks" class="space-y-5" style="scroll-margin-top: calc(var(--nav-h, 0px) + 5rem)">
  <div class="space-y-1">
    <h2 class="text-2xl font-bold text-heading">Tasks</h2>
    <p class="text-muted text-sm">Task cards, the stage badges, and the board's status chips.</p>
  </div>
  <div class="card p-8 text-center space-y-3" data-stub="true">
    <span class="badge" style="color: var(--color-cta); border-color: var(--color-cta)">Next slice</span>
    <p class="text-heading font-semibold">This section lands in the next build.</p>
    <p class="text-muted text-sm max-w-md mx-auto">
      Wave 2 fills it with live task cards, the stage-* badge palette, and the board status chips.
    </p>
  </div>
</section>
