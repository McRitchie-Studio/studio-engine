Someone asked to change the email on your <%= @app_name %> account.

  From: <%= @current_email %>
  To:   <%= @new_email %>

If that was you, open this link to approve it:

<%= @confirm_url %>

This link expires in <%= (Studio::EmailChangeToken::TTL / 60) %> minutes and can only be used once.
Until you confirm, your address stays <%= @current_email %>.

If it was not you, ignore this message — nothing will change, and your account
keeps its current address.

— <%= @app_name %>
